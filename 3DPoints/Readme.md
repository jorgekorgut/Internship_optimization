# Installation
```sudo apt install make```  
```sudo apt install g++```  

# Launching the code
For compiling, run make inside the specific folder  
```make```
For running the code  
```./build/main.exe```